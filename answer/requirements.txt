pydantic>=2.0
pydantic-ai>=1.107.0
pyyaml>=6.0                # page frontmatter parsing (the contract is YAML)
mcp>=1.2                   # the MCP server adapter (FastMCP)
uvicorn>=0.30              # http transport
pytest>=8.0
pytest-cov>=5.0
